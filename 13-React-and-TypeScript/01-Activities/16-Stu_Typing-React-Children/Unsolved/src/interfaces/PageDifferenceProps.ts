export default interface PageDifferenceProps {
  // TODO: How can we allow children to be a value that is not an element?

  //importing the type of the children into each file
  children: React.ReactNode;
}
